apiVersion: v1
kind: Service
metadata:
  name: {{ include "norcedrillingcartographicprojectionservice.fullname" . }}
  labels:
    {{- include "norcedrillingcartographicprojectionservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "norcedrillingcartographicprojectionservice.selectorLabels" . | nindent 4 }}
